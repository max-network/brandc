/**
 * @max-network/css — shared design-language contract (CSS custom properties) for the
 * Max Network UI kits. See {@link "./theme".CONTRACT} for the variable vocabulary and
 * {@link "./theme".THEME_CSS} for the default (Max Health) brand.
 */
export { CONTRACT, THEME_CSS, MAXHEALTH_CSS, type TokenName } from "./theme.js";
